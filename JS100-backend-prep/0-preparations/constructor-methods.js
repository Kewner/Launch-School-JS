
// Constructors are factories that create a values of a particular type,
// representing a blueprint for a data type with the same name. The String
// constructor is a factory that creates values of type string.

// Methods are functions that need a value that you can use to call the
// function. There are 2 types of methods:

// - Instance methods: Constructor.prototype.methodName()
//   'Hello, '.concat('Bob!')

// - Static methods: Constructor.methodName()
//   Uses the constructor name (String) instead of a value:
//   String.fromCharCode(97)