// Write a program name greeter.js that greets 'Victor' three times. Your program
// should use a variable and not hard code the string value 'Victor' in each 
// greeting.

let name = 'Victor';

console.log(`Hi ${name}.`);
console.log(`Hi ${name}.`);
console.log(`Hi ${name}.`);