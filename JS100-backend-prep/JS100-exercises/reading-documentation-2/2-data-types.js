// Without looking into your notes, try to remember which primitive data types
// are defined in JavaScript. Then find this information on MDN.

string
number
boolean
null
undefined
symbol
(bigint)

// https://developer.mozilla.org/en-US/docs/Glossary/Primitive

// There are 6 primitive data types: string, number, bigint, boolean, undefined,
// and symbol. There also is null, which is seemingly primitive, but indeed is a
// special case for every Object: and any structured type is derived from null by
// the Prototype Chain.