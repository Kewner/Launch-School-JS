// Predict the output of the following code:

if (false || true) {
  console.log('Yes!');
} else {
  console.log('No...');
}

// This will log 'Yes!'
// For (false || true) to be evaluated as true, one of the expressions has
// to evaluate to true, which true does.