// What will the following code log to the console and why? Don't run it until
// you have tried to answer.

if (true) {
  let myValue = 20;
}

console.log(myValue);

// This will raise a ReferenceError, because we're trying to use a value outside
// of its scope, which is the if code block.