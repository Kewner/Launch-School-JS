// What will the following code log to the console and why? Don't run it
// until you have tried to answer.

let a = 1;

function myFunction() {
  console.log(a);
}

myFunction();

// This will log 1
// a is a global variable, so it's accessible from inside the if block.